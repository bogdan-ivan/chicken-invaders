#include "pch.h"
#include "CppUnitTest.h"
#include "../Chicken Invaders/Wave.h";

using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace ChickenInvadersTest
{
	TEST_CLASS(WaveTest)
	{
	public:

		TEST_METHOD(Constructor)
		{ 


			/*Wave wave1(5, Pattern::HORIZONTAL, Shape::CIRCLE);
			Assert::IsTrue(wave1.getEnemyNumber() == 5);
			Assert::IsTrue(wave1.getPattern() == Pattern::HORIZONTAL);
			Assert::IsTrue(wave1.getShape() == Shape::CIRCLE);*/

		}
	};
}
